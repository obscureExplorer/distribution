<?xml version="1.0" encoding="UTF-8"?>
<solver>
    <!-- Domain model configuration -->
    <scanAnnotatedClasses>
        <packageInclude>domain</packageInclude>
    </scanAnnotatedClasses>

    <!--<environmentMode>FULL_ASSERT</environmentMode>-->

    <!-- Score configuration -->
    <scoreDirectorFactory>

<!--        <assertionScoreDirectorFactory>
            <scoreDrl>config/rules.drl</scoreDrl>
        </assertionScoreDirectorFactory>-->
        <scoreDrl>config/rules.drl</scoreDrl>
        <initializingScoreTrend>ONLY_DOWN</initializingScoreTrend>
    </scoreDirectorFactory>

    <!-- Optimization algorithms configuration -->
    <termination>
        <minutesSpentLimit>2</minutesSpentLimit>
<!--        <termination>
            <secondsSpentLimit>10</secondsSpentLimit>
        </termination>-->
        <!--<bestScoreLimit>0hard/-100soft</bestScoreLimit>-->
    </termination>

<!--    <localSearch>
        <termination>
        <unimprovedStepCountLimit>100</unimprovedStepCountLimit>
        </termination>
     </localSearch>-->

    <constructionHeuristic>
        <constructionHeuristicType>FIRST_FIT_DECREASING</constructionHeuristicType>
        <!--<constructionHeuristicType>FIRST_FIT</constructionHeuristicType>-->
    </constructionHeuristic>
     <localSearch>
       <unionMoveSelector>
         <changeMoveSelector/>
         <swapMoveSelector>
           <filterClass>filter.DifferentCourseSwapMoveFilter</filterClass>
         </swapMoveSelector>
       </unionMoveSelector>
       <acceptor>
         <lateAcceptanceSize>600</lateAcceptanceSize>
       </acceptor>
       <forager>
         <acceptedCountLimit>4</acceptedCountLimit>
       </forager>
     </localSearch>
</solver>
